<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Sign in</title>
  <style>
    :root { --bg:#0b0f19; --card:#121a2a; --border:#23304d; --text:#e8eefc; --muted:#a9b6d6; }
    *{box-sizing:border-box}
    body{margin:0;font-family:system-ui,-apple-system,Segoe UI,Roboto,Arial;background:radial-gradient(1200px 800px at 20% 10%, #182348 0%, var(--bg) 55%);color:var(--text)}
    .wrap{min-height:100vh;display:flex;align-items:center;justify-content:center;padding:24px}
    .card{width:100%;max-width:440px;background:rgba(18,26,42,.9);border:1px solid var(--border);border-radius:18px;padding:22px;box-shadow:0 18px 45px rgba(0,0,0,.45);backdrop-filter: blur(8px)}
    .brand{display:flex;align-items:center;gap:10px;margin-bottom:8px}
    .dot{width:12px;height:12px;border-radius:50%;background:#7aa2ff;box-shadow:0 0 18px rgba(122,162,255,.8)}
    h1{font-size:20px;margin:0}
    .sub{margin:6px 0 18px;color:var(--muted);font-size:13px;line-height:1.4}
    .row{display:flex;flex-direction:column;gap:8px;margin-bottom:12px}
    label{font-size:12px;color:var(--muted)}
    input{
      width:100%;padding:12px 12px;border-radius:12px;border:1px solid var(--border);
      background:#0e1527;color:var(--text);outline:none;
    }
    input:focus{border-color:#4f7cff;box-shadow:0 0 0 3px rgba(79,124,255,.18)}
    .btn{
      width:100%;padding:12px 14px;border-radius:12px;border:1px solid var(--border);
      background:#4f7cff;color:#0b0f19;font-weight:700;cursor:pointer;
    }
    .btn:hover{filter:brightness(.95)}
    .btnGhost{
      width:100%;padding:12px 14px;border-radius:12px;border:1px solid var(--border);
      background:#ffffff;color:#0b0f19;text-decoration:none;font-weight:700;display:flex;align-items:center;justify-content:center;gap:10px;
    }
    .btnGhost:hover{filter:brightness(.96)}
    .split{display:flex;align-items:center;gap:10px;margin:14px 0}
    .split:before,.split:after{content:"";flex:1;height:1px;background:rgba(35,48,77,.9)}
    .split span{font-size:12px;color:var(--muted)}
    .err{background:rgba(255,77,77,.12);border:1px solid rgba(255,77,77,.35);padding:10px 12px;border-radius:12px;margin-bottom:12px;color:#ffd2d2;font-size:13px}
    .foot{margin-top:12px;color:var(--muted);font-size:12px;text-align:center}
    .g{width:18px;height:18px}
    .meta{display:flex;justify-content:space-between;align-items:center;margin:8px 0 14px}
    .chk{display:flex;gap:8px;align-items:center;color:var(--muted);font-size:12px}
    .link{color:#bcd0ff;text-decoration:none;font-size:12px}
  </style>
</head>
<body>
  <div class="wrap">
    <div class="card">
      <div class="brand">
        <div class="dot"></div>
        <h1>Sign in</h1>
      </div>
      <div class="sub">Sign in with your username/password or continue with Google.</div>

      <% if (error) { %>
        <div class="err"><%= error %></div>
      <% } %>

      <form method="POST" action="/login">
        <input type="hidden" name="next" value="<%= next || '/dashboard' %>"/>

        <div class="row">
          <label for="username">Username or email</label>
          <input id="username" name="username" autocomplete="username" required />
        </div>

        <div class="row">
          <label for="password">Password</label>
          <input id="password" name="password" type="password" autocomplete="current-password" required />
        </div>

        <div class="meta">
          <label class="chk">
            <input type="checkbox" name="remember" value="1" />
            Remember me
          </label>
          <a class="link" href="/auth/google?next=<%= encodeURIComponent(next || '/dashboard') %>">Use Google instead</a>
        </div>

        <button class="btn" type="submit">Sign in</button>
      </form>

      <div class="split"><span>or</span></div>

      <a class="btnGhost" href="/auth/google?next=<%= encodeURIComponent(next || '/dashboard') %>">
        <svg class="g" viewBox="0 0 48 48" aria-hidden="true">
          <path fill="#FFC107" d="M43.6 20.5H42V20H24v8h11.3C33.7 32.6 29.3 36 24 36c-6.6 0-12-5.4-12-12s5.4-12 12-12c3.1 0 5.9 1.2 8 3.1l5.7-5.7C34.2 6.5 29.4 4.5 24 4.5 12.7 4.5 3.5 13.7 3.5 25S12.7 45.5 24 45.5 44.5 36.3 44.5 25c0-1.5-.2-3-.9-4.5z"/>
          <path fill="#FF3D00" d="M6.3 14.7l6.6 4.8C14.7 16.2 19 12 24 12c3.1 0 5.9 1.2 8 3.1l5.7-5.7C34.2 6.5 29.4 4.5 24 4.5c-7.9 0-14.8 4.6-17.7 11.2z"/>
          <path fill="#4CAF50" d="M24 45.5c5.3 0 10.2-2 13.9-5.3l-6.4-5.4C29.5 36.1 26.9 37 24 37c-5.2 0-9.6-3.4-11.2-8.1l-6.6 5.1C9.1 41 16 45.5 24 45.5z"/>
          <path fill="#1976D2" d="M44.5 25c0-1.5-.2-3-.9-4.5H24v8h11.3c-1.1 3.1-3.4 5.5-6.3 6.9l6.4 5.4c3.7-3.4 6.1-8.4 6.1-15.8z"/>
        </svg>
        Continue with Google
      </a>

      <div class="foot">Protected access for Square Inventory Sync</div>
    </div>
  </div>
</body>
</html>
